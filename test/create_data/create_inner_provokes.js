'use strict';

/**
 * 创建公司内部挑战
 * @param {Object} company
 * @param {Array} teams 公司所有小队
 * @param {Function} callback 形式为function(err, campaigns){}
 */
var createInnerProvokes = function (company, teams, callback) {
  // todo
};

module.exports = createInnerProvokes;