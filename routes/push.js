'use strict';
var token = require('../services/token');
module.exports = function (app, ctrl) {

  // app.get('/push/:userId',token.needToken, ctrl.getPushStatus);
  // app.put('/push/:userId', token.needToken, ctrl.updatePushStatus);
};